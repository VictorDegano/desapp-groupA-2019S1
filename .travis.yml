language: java

sudo: required

jdk: oraclejdk11

cache:
  directories:
    - $HOME/.m2

script:
  - cd desapp-grupoA-2019S1
  - mvn clean install -Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8 -Dmaven.javadoc.skip=true -B -V

#after_success:
# Aun no se hace el deploy a heroku
# deploy:
#   - provider: heroku
#     skip_cleanup: true
#     api_key:
#     app:
